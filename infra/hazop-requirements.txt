# Runtime dependencies for the HAZOP backend on App Service.
#
# The repo-root requirements.txt is a full pip freeze of the dev machine and
# drags in torch, transformers and deepeval - gigabytes that this service never
# imports. This is the actual import closure of backend/, top-level only, with
# transitive deps left to the resolver.
flask==3.1.2
flask-cors==6.0.1
Flask-SocketIO==5.5.1
gunicorn==23.0.0
openai==1.93.0
pandas==2.3.1
openpyxl==3.1.5
azure-storage-blob==12.24.0
langchain==0.3.26
langchain-community==0.3.26
python-dotenv==1.1.1
coloredlogs==15.0.1
# assistant file uploads
pypdf==6.1.1
python-docx==1.2.0
python-pptx==1.0.2
